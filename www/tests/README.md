## Exécution des tests unitaires

Utilisez la commande suivante depuis la racine du projet pour lancer la suite PHPUnit :

```bash
./vendor/bin/phpunit --configuration phpunit.xml
```

Assurez-vous d’avoir installé les dépendances via `composer install` avant d’exécuter les tests.